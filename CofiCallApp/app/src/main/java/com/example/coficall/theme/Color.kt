package com.example.coficall.theme

import androidx.compose.ui.graphics.Color

// === Coficab Brand Colors ===
val CoficabBlue = Color(0xFF0D1B3E)       // Azul escuro principal
val CoficabBlueMid = Color(0xFF1A2D5A)    // Azul médio
val CoficabBlueSurface = Color(0xFF1E3366) // Superfície azul
val CoficabYellow = Color(0xFFFFC107)      // Amarelo destaque (aviso offline, CTA)
val CoficabYellowDark = Color(0xFFE6A800)  // Amarelo escuro (hover/pressed)

// === Neutrals ===
val NeutralWhite = Color(0xFFFFFFFF)
val NeutralOffWhite = Color(0xFFF5F7FA)
val NeutralLightGrey = Color(0xFFE8ECF2)
val NeutralMedGrey = Color(0xFF9AA3B2)
val NeutralDarkGrey = Color(0xFF4A5568)
val NeutralCharcoal = Color(0xFF1A202C)

// === Dark Theme Surfaces ===
val DarkBackground = Color(0xFF0A1628)
val DarkSurface = Color(0xFF0F2040)
val DarkSurfaceVariant = Color(0xFF162845)
val DarkCard = Color(0xFF1A2D5A)

// === Semantic Colors ===
val OnlineGreen = Color(0xFF34D399)
val OfflineOrange = Color(0xFFF59E0B)
val ErrorRed = Color(0xFFEF4444)
val StarYellow = Color(0xFFFFD700)

// === Light Theme Visual Redesign Colors ===
val CoficabRoyalBlue = Color(0xFF0056C6)     // Azul Royal para botões e destaques claros
val LightOfflineBg = Color(0xFFFFDEDE)       // Fundo rosa de aviso offline no início
val LightOfflineText = Color(0xFF8A1F1F)     // Texto vermelho do aviso offline
val LightOrangeBg = Color(0xFFFEF3C7)        // Fundo bege/laranja para aviso offline de Favoritos
val LightOrangeText = Color(0xFFB45309)      // Texto laranja para aviso offline de Favoritos
val LightGrayBorder = Color(0xFFE5E7EB)      // Borda fina dos cards (Tailwind gray-200)
val LightGrayBg = Color(0xFFF3F4F6)          // Fundo cinza para tabs/inputs
val LightBluePillBg = Color(0xFFDBEAFE)      // Fundo azul claro para pílulas de ação e botões (Tailwind blue-100)
val LightBluePillText = Color(0xFF1E40AF)    // Ícones e textos azul escuro nas pílulas

